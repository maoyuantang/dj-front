# Local simulator only. WeChat real devices cannot access the developer computer via localhost.
VITE_API_BASE_URL=http://localhost:3000
# Replace these placeholders before real integration.
# VITE_API_BASE_URL_TEST=https://replace-with-test-api.example.com
# VITE_API_BASE_URL_PROD=https://replace-with-production-api.example.com
