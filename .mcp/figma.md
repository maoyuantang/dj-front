# Figma / MasterGo MCP

当前未配置设计 MCP。未来接入前确认客户端/Server 官方说明、工作区授权、只读范围、目标设计文件、版本、Token 存储和素材许可证；不要把访问凭证或受限素材写入仓库。

成功验证后在 [`docs/design/README.md`](../docs/design/README.md) 记录可访问链接、版本和验收范围。此清单本身不证明 AI 已能读取设计稿。
