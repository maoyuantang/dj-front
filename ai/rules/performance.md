# 性能规则

保持首屏和主包精简；只有真实页面和产品路径确认后才按业务域配置分包/预下载，不制造空分包。图片使用真实尺寸和合适格式，长列表避免无界渲染，监听/定时器/图表/请求在页面离开时释放；重复 GET 按真实场景取消、合并或缓存。

优化前先测量包体积、首屏、接口或渲染瓶颈，不做没有证据的微优化。当前仓库尚未建立包体预算、性能基线、图片流水线或自动平台测量，这些只能作为建议，不能写成已启用能力。性能改变需记录目标、基线、方法和前后结果。
